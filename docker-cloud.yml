web:
  # Replace username/repo:tag with your name and image details.
  image: 'robbybro/robbybro:latest'

  # Redeploy web app in case if it's image is updated.
  autoredeploy: true

  # Expose port 8080 of the container and map it to host's port 8080
  ports:
    - '8080:8080'
